const loginRoutes = require('./routeLogin');
const userRouter = require('./userRouter');
const categorieRouter = require('./categorieRouter');

module.exports = {
  loginRoutes,
  userRouter,
  categorieRouter,
};
